import { ComponentFixture, TestBed } from '@angular/core/testing';
import { DrtradddatapopupPage } from './drtradddatapopup.page';

describe('DrtradddatapopupPage', () => {
  let component: DrtradddatapopupPage;
  let fixture: ComponentFixture<DrtradddatapopupPage>;

  beforeEach(() => {
    fixture = TestBed.createComponent(DrtradddatapopupPage);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
