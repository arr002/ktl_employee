import { ComponentFixture, TestBed } from '@angular/core/testing';
import { NewsetclientcustPage } from './newsetclientcust.page';

describe('NewsetclientcustPage', () => {
  let component: NewsetclientcustPage;
  let fixture: ComponentFixture<NewsetclientcustPage>;

  beforeEach(() => {
    fixture = TestBed.createComponent(NewsetclientcustPage);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
