import { ComponentFixture, TestBed } from '@angular/core/testing';
import { NewsetclientempPage } from './newsetclientemp.page';

describe('NewsetclientempPage', () => {
  let component: NewsetclientempPage;
  let fixture: ComponentFixture<NewsetclientempPage>;

  beforeEach(() => {
    fixture = TestBed.createComponent(NewsetclientempPage);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
