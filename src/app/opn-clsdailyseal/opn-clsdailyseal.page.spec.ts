import { ComponentFixture, TestBed } from '@angular/core/testing';
import { OpnClsdailysealPage } from './opn-clsdailyseal.page';

describe('OpnClsdailysealPage', () => {
  let component: OpnClsdailysealPage;
  let fixture: ComponentFixture<OpnClsdailysealPage>;

  beforeEach(() => {
    fixture = TestBed.createComponent(OpnClsdailysealPage);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
