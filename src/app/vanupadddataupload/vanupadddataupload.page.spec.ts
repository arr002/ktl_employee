import { ComponentFixture, TestBed } from '@angular/core/testing';
import { VanupadddatauploadPage } from './vanupadddataupload.page';

describe('VanupadddatauploadPage', () => {
  let component: VanupadddatauploadPage;
  let fixture: ComponentFixture<VanupadddatauploadPage>;

  beforeEach(() => {
    fixture = TestBed.createComponent(VanupadddatauploadPage);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
